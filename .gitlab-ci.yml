deploy:
  before_script:
    - chmod 400 $SSH_KEY
    - mkdir -p ~/.ssh
    - ssh-keyscan -H 34.64.229.17 >> ~/.ssh/known_hosts
  script:
    - ssh -i $SSH_KEY elice@34.64.229.17 "cat /etc/os-release"
    - cat /etc/os-release
  tags:
    - deploy